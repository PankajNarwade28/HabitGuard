# 🏗️ ML System Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                      HabitGuard App (React Native)              │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Home Tab   │  │ Progress Tab │  │ Profile Tab  │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                   │
│         └──────────────────┴──────────────────┘                   │
│                            │                                       │
│                            ▼                                       │
│                  ┌─────────────────┐                             │
│                  │   MLService.ts   │                             │
│                  └────────┬─────────┘                             │
│                           │                                       │
└───────────────────────────┼───────────────────────────────────────┘
                            │
                            │ HTTP/REST API
                            │
┌───────────────────────────┼───────────────────────────────────────┐
│                Backend (Node.js/Express)                          │
├───────────────────────────┼───────────────────────────────────────┤
│                           ▼                                       │
│                  ┌─────────────────┐                             │
│                  │  ML Routes       │                             │
│                  │  /api/ml/*       │                             │
│                  └────────┬─────────┘                             │
│                           │                                       │
│              ┌────────────┼────────────┐                          │
│              ▼            ▼            ▼                          │
│      ┌──────────┐  ┌──────────┐  ┌──────────┐                   │
│      │ /predict │  │ /student │  │ /report  │                   │
│      └────┬─────┘  └────┬─────┘  └────┬─────┘                   │
│           │             │             │                           │
└───────────┼─────────────┼─────────────┼───────────────────────────┘
            │             │             │
            │             │             │ Python Shell / Subprocess
            │             │             │
┌───────────┼─────────────┼─────────────┼───────────────────────────┐
│        ML Analysis Layer (Python)                                 │
├───────────┼─────────────┼─────────────┼───────────────────────────┤
│           ▼             ▼             ▼                           │
│    ┌──────────────────────────────────────────┐                  │
│    │     usage_predictor.py                    │                  │
│    ├──────────────────────────────────────────┤                  │
│    │                                            │                  │
│    │  ┌─────────────────────────────────┐     │                  │
│    │  │  NeuralUsagePredictor Class     │     │                  │
│    │  ├─────────────────────────────────┤     │                  │
│    │  │ • build_model()                  │     │                  │
│    │  │ • train_model()                  │     │                  │
│    │  │ • predict()                      │     │                  │
│    │  │ • check_student_restrictions()   │     │                  │
│    │  │ • save_model()                   │     │                  │
│    │  │ • load_model()                   │     │                  │
│    │  └─────────────────────────────────┘     │                  │
│    │                                            │                  │
│    │  ┌─────────────────────────────────┐     │                  │
│    │  │  HabitGuardMLAnalyzer Class     │     │                  │
│    │  ├─────────────────────────────────┤     │                  │
│    │  │ • analyze_patterns()             │     │                  │
│    │  │ • generate_pdf_report()          │     │                  │
│    │  │ • generate_txt_report()          │     │                  │
│    │  │ • predict_future_usage()         │     │                  │
│    │  └─────────────────────────────────┘     │                  │
│    └──────────────────────────────────────────┘                  │
│                           │                                       │
│                           ▼                                       │
│    ┌──────────────────────────────────────────┐                  │
│    │         TensorFlow Model                  │                  │
│    │         (usage_nn_model.h5)               │                  │
│    └──────────────────────────────────────────┘                  │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

## Data Flow

### 1. Prediction Request Flow

```
User Opens App
    │
    ▼
App collects usage data
    │
    ▼
MLService.getPrediction(usageData)
    │
    ▼
POST /api/ml/predict
    │
    ▼
Backend executes Python script
    │
    ▼
NeuralUsagePredictor.predict()
    │
    ├─► Load TensorFlow model
    ├─► Process 10 features
    ├─► Run inference
    └─► Generate suggestions
    │
    ▼
Return JSON response
    │
    ▼
Display in UI component
```

### 2. Student Mode Check Flow

```
User enables Student Mode
    │
    ▼
App monitors usage continuously
    │
    ▼
MLService.checkStudentRestrictions(usageData)
    │
    ▼
POST /api/ml/student-check
    │
    ▼
NeuralUsagePredictor.check_student_restrictions()
    │
    ├─► Compare against thresholds
    ├─► Calculate violations
    ├─► Generate blocked apps list
    └─► Calculate productivity score
    │
    ▼
Return restrictions object
    │
    ▼
If violations exist:
    ├─► Show alert notification
    ├─► Display violation details
    ├─► Block suggested apps
    └─► Show recommendations
```

### 3. Report Generation Flow

```
User taps "Generate Report"
    │
    ▼
User selects format (PDF/TXT)
    │
    ▼
MLService.generateReport(format, csvFile)
    │
    ▼
POST /api/ml/generate-report
    │
    ▼
HabitGuardMLAnalyzer.generate_xxx_report()
    │
    ├─► Load CSV data
    ├─► Analyze patterns
    ├─► Generate predictions
    └─► Create formatted report
    │
    ▼
Return file path
    │
    ▼
Backend sends file download
    │
    ▼
App receives file
    │
    ▼
User can share/view report
```

## Neural Network Architecture

```
Input Layer (10 neurons)
    │
    │  [social_media, entertainment, productivity,
    │   communication, gaming, browsing, education,
    │   shopping, news, other]
    │
    ▼
┌──────────────────────────┐
│   Dense Layer (64)       │
│   Activation: ReLU       │
└────────────┬─────────────┘
             │
             ▼
┌──────────────────────────┐
│   Dropout (0.3)          │
└────────────┬─────────────┘
             │
             ▼
┌──────────────────────────┐
│   Dense Layer (32)       │
│   Activation: ReLU       │
└────────────┬─────────────┘
             │
             ▼
┌──────────────────────────┐
│   Dropout (0.2)          │
└────────────┬─────────────┘
             │
             ▼
┌──────────────────────────┐
│   Dense Layer (16)       │
│   Activation: ReLU       │
└────────────┬─────────────┘
             │
             ▼
┌──────────────────────────┐
│   Output Layer (3)       │
│   Activation: Softmax    │
└────────────┬─────────────┘
             │
             ▼
    [No Change ✅, Reduce Usage ⚠️, Take Breaks 🌟]
```

## Student Mode Decision Tree

```
                    Check Student Mode
                            │
                    ┌───────┴───────┐
                    ▼               ▼
            is_student=True   is_student=False
                    │               │
                    ▼               ▼
        Check each category   Return "Not in
        against thresholds    student mode"
                    │
        ┌───────────┴───────────┬───────────────┬──────────────┐
        ▼                       ▼               ▼              ▼
  Social Media          Entertainment      Gaming         Browsing
   (limit: 2h)           (limit: 3h)     (limit: 1.5h)  (limit: 2h)
        │                       │               │              │
        ▼                       ▼               ▼              ▼
  actual > limit?        actual > limit?  actual > limit? actual > limit?
        │                       │               │              │
    ┌───┴───┐              ┌───┴───┐      ┌───┴───┐     ┌───┴───┐
    ▼       ▼              ▼       ▼      ▼       ▼     ▼       ▼
   Yes     No             Yes     No     Yes     No    Yes     No
    │                      │              │             │
    ▼                      ▼              ▼             ▼
Add violation         Add violation   Add violation  Add violation
    │                      │              │             │
    └──────────────────────┴──────────────┴─────────────┘
                            │
                            ▼
                  Calculate severity
                            │
                ┌───────────┼───────────┐
                ▼           ▼           ▼
           violations    violations   violations
              >= 3         == 2        == 1
                │           │           │
                ▼           ▼           ▼
             HIGH        MEDIUM       LOW
                │           │           │
                └───────────┴───────────┘
                            │
                            ▼
              Generate blocked apps list
                            │
                            ▼
              Generate recommendations
                            │
                            ▼
             Calculate productivity score
                            │
                            ▼
                Return restrictions object
```

## Report Generation Pipeline

```
                 User Data (CSV/JSON)
                          │
                          ▼
            ┌─────────────────────────┐
            │  HabitGuardMLAnalyzer   │
            │  .load_csv_data()       │
            └────────────┬────────────┘
                         │
                         ▼
            ┌─────────────────────────┐
            │  .analyze_patterns()    │
            ├─────────────────────────┤
            │ • Calculate summary     │
            │ • Detect trends         │
            │ • Classify behavior     │
            │ • Generate predictions  │
            │ • Create recommendations│
            └────────────┬────────────┘
                         │
         ┌───────────────┴───────────────┐
         ▼                               ▼
┌──────────────────┐         ┌──────────────────┐
│ generate_pdf()   │         │ generate_txt()   │
├──────────────────┤         ├──────────────────┤
│ 1. Import        │         │ 1. Format        │
│    reportlab     │         │    plain text    │
│                  │         │                  │
│ 2. Create        │         │ 2. Add sections  │
│    document      │         │    with emojis   │
│                  │         │                  │
│ 3. Add title     │         │ 3. Write to file │
│    + timestamp   │         │                  │
│                  │         │ 4. Return path   │
│ 4. Build tables  │         │                  │
│                  │         └──────────────────┘
│ 5. Add styling   │
│                  │
│ 6. Add sections: │
│    - Summary     │
│    - Behavior    │
│    - Predictions │
│    - Recommend.  │
│                  │
│ 7. Generate PDF  │
│                  │
│ 8. Return path   │
└──────────────────┘
```

## File Structure

```
HabitGuard/
│
├── ml_analysis/
│   ├── usage_predictor.py        ← Main ML script
│   ├── requirements.txt           ← Python dependencies
│   ├── README.md                  ← Documentation
│   │
│   ├── models/                    ← Saved models
│   │   └── usage_nn_model.h5     ← TensorFlow model
│   │
│   └── reports/                   ← Generated reports
│       ├── *.pdf
│       └── *.txt
│
├── backend/
│   ├── routes/
│   │   └── ml.js                 ← ML API endpoints
│   │
│   └── index.js                  ← Express server
│
├── services/
│   └── MLService.ts              ← React Native ML service
│
└── components/
    ├── PredictionCard.tsx        ← Prediction UI
    ├── StudentModeAlert.tsx      ← Student alerts
    └── ReportGenerator.tsx       ← Report UI
```

## Technology Stack

### Frontend (React Native)
```
┌─────────────────────────────────┐
│ React Native (Expo)             │
│ TypeScript                      │
│ NativeWind (Tailwind CSS)       │
│ Expo Router                     │
└─────────────────────────────────┘
```

### Backend (Node.js)
```
┌─────────────────────────────────┐
│ Express.js                      │
│ PythonShell / child_process     │
│ CORS, Body Parser              │
└─────────────────────────────────┘
```

### ML Layer (Python)
```
┌─────────────────────────────────┐
│ TensorFlow 2.15+                │
│ scikit-learn                    │
│ pandas, numpy                   │
│ reportlab                       │
│ matplotlib                      │
└─────────────────────────────────┘
```

## API Endpoints

### Prediction Endpoint
```
POST /api/ml/predict

Request:
{
  "usageData": {
    "social_media_hours": 3.5,
    "entertainment_hours": 2.0,
    ...
  }
}

Response:
{
  "prediction_class": 1,
  "class_name": "Reduce Usage ⚠️",
  "confidence": 0.875,
  "probabilities": {...},
  "suggestion": {
    "title": "Time to Cut Back",
    "message": "...",
    "tips": [...]
  }
}
```

### Student Check Endpoint
```
POST /api/ml/student-check

Request:
{
  "usageData": {...},
  "isStudent": true
}

Response:
{
  "restricted": true,
  "violations": [...],
  "recommendations": [...],
  "severity": "high",
  "blocked_apps": [...],
  "productivity_score": 45.3
}
```

### Report Generation Endpoint
```
POST /api/ml/generate-report

Request:
{
  "format": "pdf",
  "csvFile": "path/to/data.csv"
}

Response:
File download (application/pdf or text/plain)
```

---

**This architecture ensures scalability, maintainability, and easy integration!**
